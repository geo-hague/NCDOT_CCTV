name: Scrape Camera Credentials (weekly)

on:
  schedule:
    # Weekly, Sunday 08:00 UTC. Credentials (sourceId/systemSourceId/UUID) are
    # stable for days, so this rarely needs to run. The fast token minting is a
    # separate, frequent workflow (mint-tokens.yml).
    - cron: '0 8 * * 0'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Puppeteer
        run: npm install puppeteer
      - name: Scrape credentials
        run: node scrape-auth.js
      - name: Upload debug artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scrape-debug
          path: '*.png'
          if-no-files-found: ignore
      - name: Commit camera-auth.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add camera-auth.json
          git diff --staged --quiet && echo "No changes" || git commit -m "Update camera credentials"
          git push
